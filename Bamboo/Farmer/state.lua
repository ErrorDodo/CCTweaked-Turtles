IsBusy = false
AreaAround = {
    front = BlockData,
    top = BlockData,
    back = BlockData,
    under = BlockData,
    left = BlockData,
    right = BlockData
}

return {
    IsBusy = IsBusy,
    AreaAround = AreaAround
}